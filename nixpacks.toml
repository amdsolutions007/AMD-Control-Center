# Nixpacks Configuration for Railway
# Auto-detect Python, add Node.js for PM2, add ffmpeg

[phases.setup]
aptPkgs = ["ffmpeg", "nodejs", "npm"]

[start]
cmd = "npm install -g pm2 && pm2-runtime start ecosystem.railway.js"
