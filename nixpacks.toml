# Nixpacks Configuration for Railway
# Let Nixpacks auto-detect Python from requirements.txt
# Only specify additional packages needed

[phases.setup]
aptPkgs = ["ffmpeg"]

[start]
cmd = "npm install -g pm2 && pm2-runtime start ecosystem.railway.js"
