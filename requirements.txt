google-api-python-client>=2.39.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=0.4.6

# Optional but useful for local development
# pip install -r requirements.txt
moviepy>=2.0.0
gTTS>=2.3.0
python-dotenv>=1.0.0
# Use a google-ads version compatible with Python 3.12
google-ads>=23.0.0
streamlit>=1.25.0
pandas>=2.2.0
requests>=2.31.0
facebook-business>=19.0.0

# Social engine media generation
openai>=1.0.0
Pillow>=10.0.0
imageio-ffmpeg>=0.4.9
python-dotenv==1.0.0

# Social media automation (amd_social_manager.py)
requests-oauthlib>=1.3.1  # Twitter OAuth
tweepy>=4.14.0  # Twitter API wrapper (alternative)
# python-telegram-bot REMOVED — SOVEREIGN SEAL (2026-03-08)
# Telegram bot framework belongs ONLY in requirements-telegram.txt (Service #1).
# Installing it here caused AMD-Control-Center (Service #4) to boot telegram_approval_bot.py
# via Nixpacks auto-detection, creating a 409 Conflict against Service #1.
# DO NOT restore this line to requirements.txt.
google-genai>=1.5.0       # Gemini image generation (Four-Pack fallback Pack 2)
