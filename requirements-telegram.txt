# Telegram Approval Bot + Leke Leke Publisher — UNIFIED SERVICE DEPENDENCIES

# Telegram bot framework — [job-queue] extra installs APScheduler which powers
# ALL scheduled jobs: DOME 60s poll, Guardian digest, daily 36-State, feed import,
# draft watchdog. Without it, job_queue is None and every scheduler is silent.
python-telegram-bot[job-queue]==20.7

# Image processing
Pillow==10.1.0

# Google GenAI SDK — Imagen 3 failover engine (replaces deprecated google-generativeai)
google-genai>=1.0.0

# OpenAI image generation
openai>=1.12.0

# Selenium browser automation (merged from ghostwriter — inline publishing)
selenium==4.16.0
undetected-chromedriver>=3.5.5
webdriver-manager==4.0.1

# Environment variables
python-dotenv==1.0.0

# HTTP requests
requests==2.31.0
